@startuml
class Product{
+Product()
+nazwa: string
+cena: float
+__eq__(other: Product): bool
+__hash__(): hash
+~Product()
}
class TooManyProductsFoundError{}
interface Serwer <<interface>>{
+{abstract}Serwer(lst:List[Product])
+product: Container[Products]
+{abstract}n_max_returned_entries(): int
+{abstract}get_entries(n_letters): List[Product]
}
class ListServer{
+ListServer()
}
class MapServer{}
class Client{
+Client()
+~Client()
+get_total_price(n_letters):Optional(float)
}
Serwer*--Product
Serwer<|..ListSerwer
Serwer<|..MapSerwer
Serwer ..> TooManyProductsFoundError : << uses >>
@enduml
